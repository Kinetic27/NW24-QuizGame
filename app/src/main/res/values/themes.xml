<!--<resources xmlns:tools="http://schemas.android.com/tools">-->
<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.NW24QuizGame" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_background</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.NW24QuizGame</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
    </style>

    <style name="Theme.NW24QuizGame" parent="Base.Theme.NW24QuizGame" />
</resources>